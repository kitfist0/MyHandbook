<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <variable
            name="item"
            type="my.handbook.billing.data.db.Product"/>
        <variable
            name="listener"
            type="my.handbook.billing.ui.SimpleProductAdapter.ProductAdapterListener"/>
    </data>

    <TextView
        android:id="@+id/item_title"
        android:layout_width="match_parent"
        android:layout_height="@dimen/drawer_item_height"
        android:paddingLeft="@dimen/grid_4"
        android:paddingRight="@dimen/grid_4"
        android:background="?attr/selectableItemBackground"
        android:gravity="center_vertical|start"
        android:onClick="@{() -> listener.onProductClicked(item)}"
        android:ellipsize="end"
        android:lines="1"
        android:text="@{item.title}"
        android:textColor="@color/color_on_primary_surface_emphasis_medium"
        android:textAppearance="?attr/textAppearanceSubtitle1"
        android:drawablePadding="@dimen/grid_4"
        app:productIcon="@{item}"
        tools:text="Product title"
        tools:drawableStartCompat="@drawable/ic_twotone_cup" />

</layout>
